(function () { 'use strict'; const byId = id => document.getElementById(id); async function load(path, token) { const res = await fetch(path, { headers: { Authorization: `Bearer ${token}` }, credentials: 'same-origin' }); if (!res.ok) throw new Error('Erişim doğrulanamadı veya veri yüklenemedi.'); return res.json(); } byId('load').addEventListener('click', async () => { const token = byId('token').value; byId('status').textContent = 'Yükleniyor...'; try { const [summary, funnel, retention, features, screens, ctas] = await Promise.all(['/summary','/funnel','/retention','/features','/screens','/ctas'].map(p => load('/api/admin/analytics' + p, token))); byId('summary').textContent = JSON.stringify(summary, null, 2); byId('funnel').textContent = JSON.stringify(funnel.funnel, null, 2); byId('retention').textContent = JSON.stringify(retention.retention, null, 2); byId('features').textContent = JSON.stringify(features.features, null, 2); byId('screens').textContent = JSON.stringify(screens.screens, null, 2); byId('ctas').textContent = JSON.stringify(ctas.ctas, null, 2); byId('status').textContent = 'Güncellendi.'; } catch (err) { byId('status').textContent = err.message; } }); })();
